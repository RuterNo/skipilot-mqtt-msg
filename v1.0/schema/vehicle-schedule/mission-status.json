{
  "$id": "https://schemas.ruter.no/tp/api/skipilot/mqtt-msg/v1.0/vehicle-schedule/mission-status.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MissionStatus",
  "type": "object",
  "required": [
    "status",
    "traceId",
    "datetime"
  ],
  "description": "Mission is bound to the vehicle it self. This is what the vehicles are instructed to do next",
  "additionalProperties": false,
  "properties": {
    "status": {
      "$id": "#/properties/status",
      "type": "string",
      "description": "Status of the mission, [PENDING, ACCEPTED, REJECTED, IN_PROGRESS, INTERRUPTED, COMPLETED]"
    },
    "traceId": {
      "$id": "#/properties/traceId",
      "type": "string",
      "description": "Alphanumeric reference generated by the publisher of the event"
    },
    "datetime": {
      "$id": "#/properties/datetime",
      "type": "string",
      "description": "ISO 8601. Reflects the current UTC time."
    }
  },
  "examples": [
    {
      "status": "PENDING",
      "traceId": "holo-121212",
      "datetime": "2021-05-19T12:45:50Z"
    }
  ]
}