{
  "$id": "https://schemas.ruter.no/tp/api/skipilot/mqtt-msg/v2.0/vehicle-schedule/vehicle-activity.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VehicleActivity",
  "type": "object",
  "required": ["id"],
  "description": "Activity is superclass for mission, pickup, dropoff, etc that a vehicle performs as an item in its schedule",
  "additionalProperties": false,
  "properties": {
    "id": {
      "$id": "#/properties/id",
      "type": "string",
      "description": "Stable alphanumeric reference that is unique"
    }
  }
}